# Web Region & Audience Analyzer

一个用于分析网页归属地区、目标受众和主要语言的多信号融合分析工具。通过 IP 地理定位、HTML 元数据、页面内容信号（货币、电话号码、社交媒体）、以及语言检测等多维度数据源，给出带置信度评分的结构化判断结果。

**面向跨境多语言网站**（如 Shopify 全球店铺、SaaS 多语言站点），不针对国内中文网站。

## 功能概览

- **地区归属判断** — 判断网页所属国家/地区（ISO 3166-1 alpha-2）
- **目标受众推断** — 生成人类可读的受众描述（如 "Japanese-speaking audience in Japan"）
- **主要语言检测** — 识别页面使用的主要语言及置信度
- **🆕 Persona 驱动分析** — 在分析前先识别目标受众，基于 Persona 特征引导信号收集和评估
- **多信号融合评分** — 综合 6 类信号源加权计算，输出 0.0–1.0 置信度分数
- **证据链完整输出** — 每项判断都附带原始证据，便于人工复核
- **跨跨境本地化优化建议** — 自动审计 hreflang、locale 声明、信号一致性、市场适配等，输出评分（A–F）和具体修复方案
- **多页面站点爬取** — 自动爬取并分析整个站点（最多 3 层深度，50 个页面），聚合全站分析结果
- **AI 内容质量分析** — 使用 LLM 深度分析页面内容，评估语言质量、文化适配度、购买习惯匹配度

## 信号源架构

```
┌─────────────────────────────────────────────────────┐
│                   Signal Fusion                      │
│                                                      │
│  ┌──────────┐  ┌──────────┐  ┌───────────────────┐  │
│  │ HTML Meta │  │ Content  │  │  IP Geolocation   │  │
│  │ Signals   │  │ Signals  │  │  (ip-api.com)     │  │
│  ├──────────┤  ├──────────┤  ├───────────────────┤  │
│  │ lang     │  │ Currency │  │ Country/City      │  │
│  │ og:locale│  │ Phone    │  │ ISP/Org           │  │
│  │ hreflang │  │ Social   │  │ CDN Detection     │  │
│  │ TLD      │  │ Media    │  │                   │  │
│  └──────────┘  └──────────┘  └───────────────────┘  │
│                                                      │
│  ┌──────────────────────────────────────────────┐    │
│  │         Language Detection                    │    │
│  │  NLP Cloud API (优先) / langdetect (离线回退)  │    │
│  └──────────────────────────────────────────────┘    │
│                                                      │
│  ══════════════════════════════════════════════════   │
│               Weighted Scoring Engine                │
│  TLD(1.0) + Lang(0.9) + Locale(0.8) + LangDet(0.6) │
│  + IP(0.4/0.2) + Currency(0.3) + Phone(0.3) + ...   │
│  ══════════════════════════════════════════════════   │
│                        ↓                             │
│              Structured JSON Output                  │
└─────────────────────────────────────────────────────┘
```

## Persona 驱动分析架构

**🎯 核心理念：** 在分析开始前先识别目标受众，基于 Persona 特征引导后续的信号收集和评估。

### 工作流程

```
┌─────────────────────────────────────────────────────────────────┐
│                Persona-Driven Analysis Flow              │
├─────────────────────────────────────────────────────────────────┤
│ 1. 受众识别                                             │
│    ├─ 用户输入 → 最高优先级                                  │
│    ├─ AI 推断 → 中等优先级                                    │
│    └─ 规则推断 → 默认回退                                    │
├─────────────────────────────────────────────────────────────────┤
│ 2. Persona 生成                                           │
│    ├─ 提取受众特征                      │
│    ├─ 识别关注点 (focus areas)                              │
│    │   • 价格敏感              │
│    │   • 移动端优先 (mobile-first)                           │
│    │   • 本地信任 (local-trust)                               │
│    └─ 创建 Persona 上下文                                     │
├─────────────────────────────────────────────────────────────────┤
│ 3. 信号收集 (Persona 驱动)                                  │
│    ├─ 基础信号 (HTML、内容、IP 等）                           │
│    ├─ 增强信号 (按 Persona 关注点优先收集)                         │
│    │   • 价格敏感 → 价格结构、折扣信息                           │
│    │   • 移动端优先 → 响应式设计、触控优化                       │
│    │   • 本地信任 → 本地评价、本地支付方式                         │
│    └─ 输出带 Persona 标注的信号                                 │
├─────────────────────────────────────────────────────────────────┤
│ 4. 评估 (Persona 标准化)                                     │
│    ├─ 基于 Persona 关注点评估匹配度                               │
│    ├─ 生成针对性的优化建议                                       │
│    └─ 输出 Persona 匹配报告                                       │
└─────────────────────────────────────────────────────────────────┘
```

### Persona 关注点说明

| 关注点 | 检测的关键词 | 优先收集的信号 | 评估标准 |
|--------|--------------|----------------|----------|
| **价格敏感** | 价格、便宜、优惠、性价比、折扣 | 价格显示、折扣标记、价值对比、结账价格 | 透明度、对比展示 |
| **移动端优先** | 手机、移动、mobile、快速、app | viewport 配置、触控元素、响应式图片 | 移动适配、快速购买 |
| **本地信任** | 本地、当地、信任、评价、保障 | 本地评价、本地支付方式、本地配送 | 信任要素可见性 |

### 优势

- ✅ **分析有针对性** - 不再盲目收集所有信号，而是根据 Persona 优先收集相关信号
- ✅ **评估更准确** - 评估标准基于实际的用户画像，而非通用的 SEO 规则
- ✅ **建议更实用** - 生成的优化建议直接针对特定人群的痛点
- ✅ **支持多维度 Persona** - 价格敏感、移动端优先、本地信任等多个关注点

### 技术实现

```python
# Persona 驱动的信号收集
persona_focus = {
    'price_sensitive': True,  # 价格敏感用户
    'mobile_first': True,      # 移动端优先用户
    'local_trust': True,      # 本地信任关注用户
}

enhanced_signals = extract_persona_enhanced_signals(
    html, text_content, persona_focus
)

# 输出增强信号
{
    'pricing': {'pricing_display': True, 'discount_display': True},
    'viewport_mobile_optimized': True,
    'trust_signals': {'security_badges': True}
}
```

## 安装

### 环境要求

- Python 3.8+

### 安装依赖（推荐）

```bash
pip install beautifulsoup4 langdetect
```

> 脚本在无依赖环境下也可运行（降级模式），但安装上述包可显著提升 HTML 解析和语言检测效果。

## 用法

### 基础分析

```bash
python3 scripts/analyze_webpage.py https://www.example.com
```

### 命令行参数

#### 基础参数

| 参数 | 说明 | 默认值 |
|------|------|--------|
| `url` | 要分析的网页 URL（必填） | — |
| `--no-ip-geo` | 禁用 IP 地理定位 | 启用 |
| `--nlpcloud-token TOKEN` | NLP Cloud API Token（更高精度语言检测） | 无 |
| `--timeout N` | HTTP 请求超时秒数 | 15 |
| `--output FILE` / `-o FILE` | 输出到文件 | JSON: stdout；Markdown: 自动保存到下载目录 |
| `--format {json,markdown}` | 输出格式 | json |
| `--no-recommendations` | 禁用跨境优化建议输出 | 启用 |

#### 多页面爬取参数

| 参数 | 说明 | 默认值 |
|------|------|--------|
| `--no-crawl` | 禁用多页面站点爬取，仅分析单页 | 禁用（即默认启用爬取）|
| `--max-depth N` | 最大爬取深度 | 3 |
| `--max-pages N` | 最大爬取页面数 | 50 |
| `--smart-crawl` | 🆕 使用 AI 驱动的智能爬取（先分析网站结构，再选择核心页面） | 禁用 |

#### AI 内容分析参数

| 参数 | 说明 | 默认值 |
|------|------|--------|
| `--ai-api-base URL` | OpenAI 兼容 API 地址（或使用 `AI_API_BASE` 环境变量） | 无 |
| `--ai-api-key KEY` | API 密钥（或使用 `AI_API_KEY` 环境变量） | 无 |
| `--ai-model MODEL` | AI 模型名称 | gpt-4o |
| `--target-audience TEXT` | 手动指定目标人群（不填则由 AI/规则自动识别） | 无 |

### 使用示例

**分析整个站点（默认多页面爬取）：**
```bash
python3 scripts/analyze_webpage.py https://www.shein.com
```

**仅分析单个页面：**
```bash
python3 scripts/analyze_webpage.py https://www.shein.com --no-crawl
```

**多页面爬取 + AI 内容分析（推荐）：**
```bash
python3 scripts/analyze_webpage.py https://www.shein.com \
  --ai-api-base https://api.openai.com/v1 \
  --ai-api-key YOUR_API_KEY
```

**🆕 智能爬取（AI 驱动，只抓取核心页面）：**
```bash
python3 scripts/analyze_webpage.py https://www.shein.com --smart-crawl \
  --ai-api-base https://api.openai.com/v1 \
  --ai-api-key YOUR_API_KEY
```

智能爬取的优势：
- **效率更高** - 先快速扫描网站结构，只抓取 AI 筛选的核心页面
- **结果更准确** - AI 基于链接文本和 URL 模式识别最重要的页面
- **节省成本** - 减少不必要的页面抓取和 AI 分析调用

**手动指定目标人群并做 persona 匹配分析：**
```bash
python3 scripts/analyze_webpage.py https://example.com \
  --target-audience "北美 25-35 岁女性快时尚用户"
```

**使用环境变量配置 AI：**
```bash
export AI_API_BASE=https://api.openai.com/v1
export AI_API_KEY=sk-your-key-here
python3 scripts/analyze_webpage.py https://www.shein.com --crawl
```

**自定义爬取参数：**
```bash
python3 scripts/analyze_webpage.py https://example.com \
  --max-depth 2 --max-pages 10 -o analysis.json
```

**导出 Markdown（默认保存到下载目录）：**
```bash
python3 scripts/analyze_webpage.py https://example.com --format markdown
```

**导出 Markdown（自定义保存路径）：**
```bash
python3 scripts/analyze_webpage.py https://example.com --format markdown -o report.md
```

**使用 NLP Cloud 增强语言检测：**
```bash
python3 scripts/analyze_webpage.py https://www.spiegel.de --nlpcloud-token YOUR_TOKEN
```

**仅分析页面内容信号（不查 IP）：**
```bash
python3 scripts/analyze_webpage.py https://www.yahoo.co.jp --no-ip-geo
```

## 输出格式

- `--format json`（默认）：输出到标准输出，可用 `-o` 保存文件。
- `--format markdown`：未指定 `-o` 时自动保存到 `~/Downloads/web-region-audience-report-<域名>-<时间戳>.md`。

```jsonc
{
  "url": "https://www.yahoo.co.jp",
  "analyzedAt": "2026-02-10T06:42:10Z",
  "result": {
    "primaryRegion": "JP",                              // ISO 国家代码
    "primaryRegionName": "Japan",                       // 国家名称
    "primaryLanguage": "ja",                            // 语言代码
    "primaryLanguageName": "Japanese",                  // 语言名称
    "likelyAudience": "Japanese-speaking audience in Japan",  // 受众描述
    "regionConfidence": 0.67,                           // 地区置信度 (0.0-1.0)
    "languageConfidence": 1.0,                          // 语言置信度
    "signalBreakdown": { "JP": 3.0 }                   // 各地区得分明细
  },
  "evidence": {
    "htmlSignals": {
      "lang": "ja",                    // <html lang="...">
      "metaLocale": null,              // <meta property="og:locale">
      "metaLanguage": null,            // <meta http-equiv="content-language">
      "metaGeoRegion": null,           // <meta name="geo.region">
      "charset": "utf-8",
      "hreflangTags": [],              // <link rel="alternate" hreflang="...">
      "tld": ".jp"                     // 域名顶级域
    },
    "contentSignals": {
      "currencyCodes": [],             // 页面中出现的货币代码 (USD, CNY...)
      "currencySymbols": [],           // 货币符号 ($, ¥, €...)
      "phoneFormats": [],              // 国际电话区号 (+86, +1...)
      "socialMediaSignals": [          // 地区性社交媒体链接
        { "domain": "line.me", "region": "JP" }
      ]
    },
    "ipGeolocation": {                 // IP 地理定位结果 (ip-api.com)
      "country": "Japan",
      "countryCode": "JP",
      "city": "Chiyoda City",
      "isp": "YAHOO"
    },
    "languageDetection": {             // 语言检测结果
      "results": [{ "lang": "ja", "confidence": 0.999999 }],
      "method": "langdetect"
    }
  },
  "errors": [],                        // 致命错误
  "warnings": [],                      // 非致命警告
  "optimization": {                    // 跨境优化建议
    "summary": {
      "score": 86,                     // 评分 (0-100)
      "grade": "A",                    // 等级 (A-F)
      "totalIssues": 3,
      "critical": 0,
      "warnings": 1,
      "info": 2
    },
    "recommendations": [
      {
        "severity": "warning",
        "category": "locale-declaration",
        "issue": "Missing og:locale meta tag...",
        "recommendation": "Add og:locale for correct social media...",
        "codeExample": "<meta property=\"og:locale\" content=\"en_US\" />"
      }
    ]
  },
  "aiContentAnalysis": {              // AI 内容质量分析（需要 --ai-api-base 和 --ai-api-key）
    "inferredProductType": "E-commerce fashion retailer",
    "languageQuality": {
      "score": 8.5,                   // 1-10 分，10 为完美母语水平
      "isNativeLevel": true,
      "machineTranslationDetected": false,
      "details": "Natural, fluent Japanese with appropriate honorifics..."
    },
    "regionFit": {
      "score": 9.0,                   // 1-10 分，10 为完美适配
      "culturallyApproriate": true,
      "issues": [],
      "strengths": ["Uses local currency JPY", "References local holidays"]
    },
    "contentProductAlignment": {
      "score": 8.0,
      "details": "Clear value proposition for target audience..."
    },
    "suggestions": [
      "Consider adding more local payment methods like PayPay",
      "Include references to Japanese fashion trends"
    ]
  },
  "personaAnalysis": {                // 人群画像与网站匹配度分析（始终输出，优先用 AI）
    "audience": {
      "source": "user_input | ai_inferred | rule_based",
      "finalAudience": "北美 25-35 岁女性快时尚用户"
    },
    "regionalPersona": {
      "personaLabel": "US price-sensitive mobile-first fashion shopper",
      "traits": ["Price-sensitive", "Mobile-first", "Trend-driven"]
    },
    "personaFit": {
      "score": 7.6,                   // 1-10 分
      "isFit": true,
      "summary": "网站整体较符合该人群预期。"
    }
  }
}
```

## 智能爬取（AI 驱动的页面选择）

### 工作原理

智能爬取通过多策略流程优化页面抓取效率，**优先使用 Sitemap**：

```
┌─────────────────────────────────────────────────────────────────┐
│              Step 1: Sitemap 发现（优先）                       │
│  ┌───────────────────────────────────────────────────────────┐  │
│  │  尝试以下位置寻找 sitemap：                                 │  │
│  │  1. robots.txt 中的 Sitemap 指令                          │  │
│  │  2. /sitemap.xml                                          │  │
│  │  3. /sitemap_index.xml                                    │  │
│  │                                                          │  │
│  │  ✅ 找到：直接使用 sitemap 中的 URL（已由站长优先级排序）   │  │
│  │  ❌ 未找到：进入 Step 2                                    │  │
│  └───────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────┘
                           ↓ (如果未找到)
┌─────────────────────────────────────────────────────────────────┐
│              Step 2: 导航栏链接提取                             │
│  ┌───────────────────────────────────────────────────────────┐  │
│  │  从首页提取导航区域链接：                                 │  │
│  │  • <nav> 标签                                            │  │
│  │  • header 区域                                           │  │
│  │  • .nav, .menu, .navbar 等类名                            │  │
│  │                                                          │  │
│  │  优先抓取：首页、产品、关于、帮助等核心页面                │  │
│  └───────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────┘
                           ↓
┌─────────────────────────────────────────────────────────────────┐
│              Step 3: AI 页面优先级分析（可选）                  │
│  ┌───────────────────────────────────────────────────────────┐  │
│  │  如果配置了 AI，对导航链接进行智能分析：                    │  │
│  │  • 识别页面类型                                           │  │
│  │  • 评估分析价值                                           │  │
│  │  • 选出 Top N 最重要的页面                                 │  │
│  │                                                          │  │
│  │  如无 AI：使用启发式规则（路径模式评分）                   │  │
│  └───────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────┘
                           ↓
┌─────────────────────────────────────────────────────────────────┐
│              Step 4: 定向抓取核心页面                             │
│  ┌───────────────────────────────────────────────────────────┐  │
│  │  只抓取选定的页面：                                       │  │
│  │  • Sitemap 模式：站长提供的 URL 列表                      │  │
│  │  • 导航模式：AI/启发式筛选的核心页面                       │  │
│  │                                                          │  │
│  │  减少不必要的请求，节省时间和 API 成本                    │  │
│  └───────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────┘
```

### 输出格式

智能爬取会在 `crawlSummary` 中添加 `siteMap` 信息：

#### Sitemap 模式
```json
{
  "crawlSummary": {
    "pagesAnalyzed": 20,
    "maxDepthReached": 1,
    "pageUrls": [...],
    "siteMap": {
      "totalLinks": 156,
      "selectedPages": 20,
      "selectionMethod": "sitemap",
      "sitemapSource": "robots.txt",
      "pageTypes": ["Sitemap-provided"],
      "reasoning": "Used sitemap from robots.txt"
    }
  }
}
```

#### 导航 + AI 模式
```json
{
  "crawlSummary": {
    "pagesAnalyzed": 20,
    "maxDepthReached": 1,
    "pageUrls": [...],
    "siteMap": {
      "totalLinks": 25,
      "selectedPages": 20,
      "selectionMethod": "ai",
      "pageTypes": ["AI-selected from navigation"],
      "reasoning": "AI analyzed navigation links and selected most representative pages"
    }
  }
}
```

### 降级策略

如果 AI 分析失败（网络错误、API 限制等），会自动降级到启发式算法：

```python
# 启发式优先级规则
high_priority = [
    '/', '/home', '/index',           # 首页
    '/products?', '/shop',            # 产品页
    '/about', '/contact',             # 关于/联系
    '/help', '/support', '/faq',      # 帮助中心
    '/pricing', '/features',          # 功能/价格
]

low_priority = [
    '/privacy', '/terms', '/legal',   # 法律页面
    '/sitemap', '/search',           # 站点地图/搜索
]
```

### 性能对比

| 模式 | 页面抓取数 | API 调用 | 总耗时 | 准确性 |
|------|----------|---------|--------|--------|
| **传统 BFS** | 50 (可能很多低价值) | 50 | ~50s | 中等 |
| **智能爬取** | 20 (精选核心) | 21 (1次选择 + 20页分析) | ~25s | 高 |

## AI 内容质量分析

启用 AI 分析后（`--ai-api-base` 和 `--ai-api-key`），工具会使用 LLM 深度分析页面内容：

### 分析维度

1. **语言质量** (languageQuality)
   - 评分 1-10（10 为完美母语水平）
   - 是否达到母语水平
   - 是否检测到机器翻译痕迹
   - 语法质量、自然度、词汇适当性评估

2. **区域适配度** (regionFit)
   - 评分 1-10（10 为完美适配目标地区）
   - 文化适应性评估
   - 具体问题列表（如：为德国受众使用美式日期格式）
   - 优点列表（如：正确使用本地货币）

3. **内容产品对齐** (contentProductAlignment)
   - 评分 1-10（10 为内容完美匹配产品定位）
   - 文案、语气、信息传递是否匹配产品/服务
   - 价值主张对目标受众是否清晰

4. **改进建议** (suggestions)
   - 可操作的本地化质量改进建议
   - 针对目标市场的具体优化方向

5. **Persona 匹配分析** (personaAnalysis)
   - 先确定目标人群（用户输入优先，否则 AI 推断）
   - 按目标地区生成人群 persona（特征、动机、痛点、购买驱动）
   - 评估网站与该 persona 的匹配度（score / isFit / summary）

### 支持的 API

- OpenAI API (gpt-4o, gpt-4-turbo, gpt-3.5-turbo)
- Azure OpenAI
- 任何 OpenAI 兼容的 API（如 Anthropic Claude via proxy）

### 使用示例

```bash
# 使用 OpenAI
python3 scripts/analyze_webpage.py https://example.com \
  --ai-api-base https://api.openai.com/v1 \
  --ai-api-key sk-your-key

# 使用环境变量
export AI_API_BASE=https://api.openai.com/v1
export AI_API_KEY=sk-your-key
python3 scripts/analyze_webpage.py https://example.com

# 指定模型
python3 scripts/analyze_webpage.py https://example.com \
  --ai-api-base https://api.openai.com/v1 \
  --ai-api-key sk-your-key \
  --ai-model gpt-4-turbo
```

## 多页面站点分析

使用 `--crawl` 参数启用多页面爬取模式：

### 工作原理

1. 从起始 URL 开始爬取
2. 提取页面中的所有链接
3. 仅爬取同域名下的页面
4. 广度优先遍历，最多 3 层深度
5. 最多分析 50 个页面
6. 每个页面独立分析，最后聚合结果

### 聚合结果

- **siteResult**: 全站地区/语言判断（综合所有页面信号）
- **siteOptimization**: 全站优化建议（聚合所有页面问题）
- **aiContentAnalysis**: AI 分析聚合（平均分数 + 综合建议）
- **personaAnalysis**: 全站人群画像与匹配度聚合结果
- **pages[]**: 每个页面的详细分析结果

### 使用场景

- 分析整个网站的本地化质量
- 检查不同页面的语言一致性
- 发现全站性的本地化问题
- 评估网站整体的跨境适配度

```

## 跨境优化建议解读

工具会对以下维度进行审计并给出评分：

1. **hreflang 配置** (Critical) — 是否正确配置多语言/多地区链接，是否有 `x-default`
2. **Locale 声明** (Critical) — `<html lang>`, `og:locale`, `content-language` 是否存在且一致
3. **市场适配度** (Warning) — 目标市场的货币、电话格式、社交媒体是否本地化，**是否支持本地支付方式**（如德国 Sofort、荷兰 iDEAL、巴西 Pix）
4. **文化适应性** (Info) — 拼写习惯（美式 vs 英式）和计量单位（公制 vs 英制）是否符合目标受众习惯
5. **用户体验 (UX)** (Critical/Warning) — 移动端视口配置、表单输入类型优化（email/tel）、图片 Alt 文本
6. **服务器/CDN** (Info) — 服务器位置是否与目标受众匹配，是否使用了 CDN
5. **字符集** (Warning) — 是否使用 UTF-8 以支持多语言

| 等级 | 分数 | 含义 |
|------|------|------|
| **A** | 80-100 | 优秀，仅有少量非关键建议 |
| **B** | 60-79 | 良好，存在一些警告项 |
| **C** | 40-59 | 及格，存在较多问题 |
| **D** | 20-39 | 较差，存在关键缺失（如无 hreflang） |
| **F** | 0-19 | 极差，基本未做本地化配置 |

## 置信度解读

| 区间 | 含义 | 典型场景 |
|------|------|----------|
| **> 0.6** | 高置信度 | 多信号一致（TLD + lang + IP + 语言检测都指向同一地区） |
| **0.3 – 0.6** | 中等置信度 | 部分信号一致，部分缺失 |
| **< 0.3** | 低置信度 | 全球化站点（.com + 英语 + CDN）、信号冲突 |

## 加权评分规则

| 信号 | 权重 | 说明 |
|------|------|------|
| TLD（域名后缀） | 1.0 | 强信号，如 `.de`→德国、`.jp`→日本 |
| `<html lang="xx-YY">` 含地区 | 0.9 | 显式声明，如 `zh-CN` |
| `<html lang="xx">` 无地区 | 0.7 | 通过语言推断地区，如 `ja`→日本 |
| `og:locale` | 0.8 | OpenGraph 地区声明 |
| `content-language` | 0.7 | HTTP-equiv 语言声明 |
| 语言检测结果 | 0.6 × conf | 统计检测，按置信度缩放 |
| IP 地理定位 | 0.4 / 0.2 | 非 CDN 为 0.4，检测到 CDN 降为 0.2 |
| 货币代码/符号 | 0.3 | 每种唯一货币 |
| 电话区号 | 0.3 | 每种唯一区号 |
| 社交媒体域名 | 0.3 | 地区性社交平台（微信、VK、LINE 等） |

## 技术特性

- **零必需依赖** — 纯 Python stdlib 即可运行，`beautifulsoup4` 和 `langdetect` 为可选增强
- **SSL 容错** — 双策略 SSL（标准 → 宽松回退），解决部分站点证书问题
- **CDN 感知** — 自动检测 Cloudflare/Akamai/Fastly 等 CDN，降低 IP 信号权重
- **CJK 优化** — 对 JS 密集型页面回退提取 `<title>` + `<meta description>` 作为文本源
- **不崩溃** — 所有子系统独立运行，任一失败不影响其他信号，错误记录到 `errors`/`warnings`

## 已验证站点

| 站点 | 检测结果 | 置信度 |
|------|----------|--------|
| baidu.com | CN / China | 0.15 (JS 重页面，信号少但正确) |
| google.com | US / United States | 0.10 (全球化站点，低但正确) |
| spiegel.de | DE / Germany | 0.64 (TLD + lang + IP + langdetect 一致) |
| yahoo.co.jp | JP / Japan | 0.67 (TLD + lang + IP + LINE + langdetect) |
| yandex.ru | RU / Russia | 0.32 (TLD + IP，重定向导致文本不足) |

## 文件结构

```
web-region-audience-analyzer/
├── SKILL.md                          # Skill 元数据与使用说明
├── scripts/
│   └── analyze_webpage.py            # 主分析脚本（可独立运行）
└── references/
    ├── api-reference.md              # IP-API / NLP Cloud 接口文档
    └── signal-patterns.md            # TLD/电话/货币/社交媒体映射表
```

## 局限性

- **CDN/代理** — 使用 CDN 的站点 IP 地理定位可能偏移（已通过降权缓解）
- **JS 渲染** — 不执行 JavaScript，SPA 类站点可能提取不到有效文本
- **多语种页面** — 当前取置信度最高的语言，不做多语种权重聚合
- **ip-api.com 限频** — 免费 API 限 45 次/分钟，批量分析需注意节流
- **全球化站点** — `.com` + 英语 + US CDN 的组合会导致低置信度（这是正确行为）
